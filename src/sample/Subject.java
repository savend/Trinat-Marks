package sample;

import java.util.ArrayList;

public class Subject extends Exam {

    /*private ArrayList<Exam> examArrayList = new ArrayList<>();

    public Subject (String markName) {
        super(markName);
    }


    @Override
    public void average() {
        double total = 0;
        for (Exam exam : examArrayList) {
            total += (exam.getMark() * exam.getCoefficient());
        }

        this.setMark(total);
    }*/
}
